﻿<div class="post-meta">
    @if (Platforms != null && Platforms.Any())
    {
        @foreach (var platform in Platforms)
        {
            <p itemprop="gamePlatform">@platform.Name</p>
        }
    }
</div>