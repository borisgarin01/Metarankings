@using Domain.Games

<!-- Фильтр по платформам -->
<div class="filter-nav">
    <div class="filter-name">Платформа</div>
    <div class="filter-menu">
        <a class="@(PlatformId is not null ? "selected" : "")" href="/games/best-games/?Year=@Year">Все</a>
        @foreach (Platform platform in Platforms)
        {
            <a class="@(PlatformId == platform.Id ? "selected" : "")"
               href="/games/best-games/?Year=@Year&PlatformId=@platform.Id">
                @platform.Name
            </a>
        }
    </div>
</div>